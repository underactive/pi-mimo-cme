{
  "name": "pi-mimo-cme",
  "version": "0.1.0",
  "description": "MiMoCode-style cross-session memory (computation / memory / evolution) for the pi coding agent",
  "type": "module",
  "private": true,
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "test": "node --test 'test/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.79.1",
    "@earendil-works/pi-coding-agent": "0.79.1",
    "@types/node": "^24.0.0",
    "typebox": "1.1.38",
    "typescript": "^5.8.0"
  }
}
